import { DialogErrorMessagePipe } from './dialog-error-message.pipe';

describe('ErrorMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new DialogErrorMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
